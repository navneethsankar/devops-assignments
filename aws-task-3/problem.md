## Problem Statement
1. Create an S3 bucket with no public access and upload files to the bucket & view the logs using CloudWatch for the uploaded files.

2. Launch two EC2 instances and connect them to an application load balancer, where the output traffic from the server must be a load balancer IP address

